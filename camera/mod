cpp(17)
dep(mx:mx)
#dep(mx:mx ion:net vulkan:vk @Vulkan)
src(-apple.mm)
src(-win.cpp)
src(-linux.cpp)

if(APPLE)
    dep(IOSurface QuartzCore Metal CoreMedia CoreFoundation Cocoa IOKit AVFoundation)
    cflags(+-fobjc-arc +-Wno-deprecated-declarations)
    src(apple.mm)
elseif(WIN32)
    src(win.cpp)
    #dep(mf mfplat mfreadwrite mfuuid strmiids quartz ole32 oleaut32 uuid)
elseif(UNIX)
    src(linux.cpp)
    dep(v4l2)
endif()
